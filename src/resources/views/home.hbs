<div class="mt-4">
  <div class="row">
    {{#each users}}
      <div class="col-md-3 col-lg-3">
        <div class="card mt-4" style="width: 18rem;">
          <img class="card-img-top" src="/uploads/{{this.image}}" alt="{{this.name}}" />
          <div class="card-body">
            <a href="users/{{this._id}}"><h5
                class="card-title"
              >{{this.name}}</h5></a>
            <p class="card-text">{{this.description}}</p>
            <a href="users/{{this._id}}" class="btn btn-primary">Xem chi tiết</a>
          </div>        </div>
      </div>
    {{/each}}
  </div>
</div>